meta {
  name: Delete host
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/api/v1/hosts/{{hostId}}
  body: none
  auth: none
}

params:path {
  hostId: {{hostId}}
}

tests {
  test("should delete host", function() {
    expect(res.status).to.equal(204);
  });
}
